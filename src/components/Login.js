import React from "react";

import { Container, Button } from "react-bootstrap";

const Login = () => {
  return (
    <Container>
      <h1>Login</h1>
      <Button>test</Button>
    </Container>
  );
};

export default Login;
